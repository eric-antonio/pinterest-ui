import { StyleSheet } from "react-native";

 export const styles = StyleSheet.create({
    container:{
        flex: 1,
        flexDirection: "row",
    },
    column:{
        flex: 1,
        // padding: 10,
        flexDirection: "column",
    }, list:{
        paddingTop: 16,
    }
});